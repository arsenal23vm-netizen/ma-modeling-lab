import type{CandidateStatus,PlaceCandidate}from"./types";import{priorityAreas}from"./osakaAreas";
const foodTypes=["restaurant","italian_restaurant","pizza_restaurant"];const nameWords=["pizza","pizzeria","ピザ","ピッツェリア","forno","trattoria","osteria"];const irrelevant=["supermarket","convenience_store","grocery_store","hotel"];
export function scoreCandidate(candidate:PlaceCandidate):PlaceCandidate{let score=0;const name=candidate.normalizedName,types=[candidate.primaryType??"",...(candidate.types??[])];if(candidate.sources.includes("text_search_direct_menu"))score+=40;if(nameWords.some(x=>name.includes(x)))score+=15;if(types.some(x=>foodTypes.includes(x)))score+=15;if(candidate.googleMapsUri)score+=5;if(priorityAreas.some(x=>candidate.formattedAddress?.includes(x.name)))score+=5;if(candidate.sourceQueries.length>1)score+=10;if(candidate.businessStatus==="CLOSED_PERMANENTLY")score-=100;if(candidate.formattedAddress&&!candidate.formattedAddress.includes("大阪"))score-=50;if(!nameWords.some(x=>name.includes(x))&&!types.some(x=>foodTypes.includes(x)))score-=30;if(types.some(x=>irrelevant.includes(x)))score-=20;score=Math.max(0,Math.min(100,score));const candidateStatus:CandidateStatus=score>=70?"likely_relevant":score>=40?"needs_manual_review":score>=20?"low_confidence":"rejected";return{...candidate,candidateScore:score,candidateStatus}}
