import type { Metadata } from "next"; import { notFound } from "next/navigation"; import { wards } from "@/data/wards"; import { getShopsByWard } from "@/lib/shops"; import { MapView } from "@/components/MapView"; import { ShopCard } from "@/components/ShopCard";
type Props={params:Promise<{wardSlug:string}>}; export function generateStaticParams(){return wards.map(w=>({wardSlug:w.slug}))}
export async function generateMetadata({params}:Props):Promise<Metadata>{const {wardSlug}=await params;const ward=wards.find(w=>w.slug===wardSlug);return ward?{title:`大阪市${ward.name}のクアトロフォルマッジ`,description:`大阪市${ward.name}でクアトロフォルマッジが食べられるお店を地図と一覧から探せます。`}:{}}
export default async function WardPage({params}:Props){const {wardSlug}=await params;const ward=wards.find(w=>w.slug===wardSlug);if(!ward)notFound();const list=getShopsByWard(ward.slug);const verified=list.filter(s=>s.verificationStatus.startsWith("verified")).length;return <div className="container ward-page"><span className="eyebrow">OSAKA / {ward.slug.toUpperCase()}</span><h1>大阪市{ward.name}で<br/>クアトロフォルマッジが食べられるお店</h1><p className="lead">{ward.name}のピザ屋・イタリアンを提供確認状況と一緒に紹介します。</p><div className="stats"><div><strong>{list.length}</strong><span>掲載店舗</span></div><div><strong>{verified}</strong><span>提供確認済み</span></div></div><MapView shops={list}/><div className="shop-grid ward-grid">{list.map(s=><ShopCard key={s.id} shop={s}/>)}</div>{!list.length&&<p className="empty">現在、この区のサンプル店舗はありません。掲載情報を募集中です。</p>}</div>}
