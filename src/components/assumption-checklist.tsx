"use client";
import { useState } from "react";
const items=["実績3期と試算表が一致している","売上を事業KPIまで分解した","Base / Upside / Downsideを定義した","運転資本を回転日数で検証した","最低現金と借入返済を設定した","Sources & Usesが一致している","BS・CFチェックがゼロである","全前提に出所と更新日がある"];
export function AssumptionChecklist(){const [done,setDone]=useState<boolean[]>(items.map(()=>false));const count=done.filter(Boolean).length;return <div className="border-t-4 border-[#102235] bg-[#f7f8f6] p-6"><div className="flex justify-between"><strong>モデル完成度チェック</strong><span className="font-mono text-[#147d73]">{count} / {items.length}</span></div><div className="my-4 h-2 bg-[#d8e0e5]"><div className="h-full bg-[#147d73] transition-all" style={{width:`${count/items.length*100}%`}}/></div>{items.map((x,i)=><label key={x} className="flex cursor-pointer gap-3 border-t border-[#d8e0e5] py-3 text-sm"><input type="checkbox" checked={done[i]} onChange={()=>setDone(d=>d.map((v,j)=>j===i?!v:v))}/><span>{x}</span></label>)}<button className="mt-3 text-xs font-bold text-[#607080]" onClick={()=>setDone(items.map(()=>false))}>チェックをリセット</button></div>}
